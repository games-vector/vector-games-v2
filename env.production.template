# Application Configuration
APP_PORT=3000
APP_ENV=production
NODE_ENV=production

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=vectorgames_user
DB_PASSWORD=CHANGE_THIS_PASSWORD
DB_DATABASE=vectorgames
DB_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD
DB_SYNCHRONIZE=false

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=CHANGE_THIS_JWT_SECRET_KEY_MIN_32_CHARS
JWT_EXPIRES_IN=7d

# Frontend Host (for login URLs - can be game-specific via DB config)
FRONTEND_HOST=your-server-ip-or-domain

# Logging
LOG_LEVEL=info
